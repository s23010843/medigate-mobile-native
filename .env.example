# Environment Variables Configuration

# API Configuration
# In development: use 'local' to load from user-data.json
# In production: set to our backend API URL
EXPO_PUBLIC_API_URL=https://api.medigate.netlify.app

# MongoDB Connection (Backend Only)
MONGODB_URI=our-mongodb-uri

# JWT Secret (Backend Only)
JWT_SECRET=our-super-secret-jwt-key-change-this-in-production

# Backend Port (Backend Only)
PORT=3000

# Environment
NODE_ENV=production #local

# Optional: Third-party Services
SENTRY_DSN=our-sentry-dsn-for-error-tracking
ANALYTICS_KEY=our-analytics-key

# Optional: Email Service (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=our-email@gmail.com
SMTP_PASS=our-email-password

# Optional: SMS Service (for notifications)
TWILIO_ACCOUNT_SID=our-twilio-account-sid
TWILIO_AUTH_TOKEN=our-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890
